<!-- <h4>Add a note: </h4>
  <%#= form_for [project, note] do |f| %>
    <%#= f.label :content %>
    <%#= f.text_area :content %>
    <%#= f.submit %>
  <%# end %> -->

<h4>AJAX Add a note: </h4>
  <%= form_for ([project, note]  ) do |f| %>
    <%= f.label :content %>
    <%= f.text_area :content %>
    <p><%= f.submit id: "ajax_submit" %></p>
  <% end %>
